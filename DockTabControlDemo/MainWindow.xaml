<Window x:Class="DockTabControlDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        d:DataContext="{d:DesignInstance local:MainWindowViewModel, IsDesignTimeCreatable=True}"
        xmlns:c="clr-namespace:DockTabControl;assembly=DockTabControl"
        xmlns:local="clr-namespace:DockTabControlDemo"
        mc:Ignorable="d"
        
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate x:Key="TabItemTemplate" DataType="{x:Type local:TabViewModel}">
            <TextBlock Text="{Binding Header, Mode=OneWay}"/>
        </DataTemplate>
        <DataTemplate x:Key="TabContentTemplate" DataType="{x:Type local:TabViewModel}">
            <TextBlock Text="{Binding Body, Mode=OneWay}"/>
        </DataTemplate>
    </Window.Resources>
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <c:DockTabControl ItemsSource="{Binding LeftTabs}"
                                       Grid.Column="0"
                                       ItemTemplate="{StaticResource TabItemTemplate}"
                                       ContentTemplate="{StaticResource TabContentTemplate}"/>
        <c:DockTabControl ItemsSource="{Binding RightTabs}"
                                       Grid.Column="2"
                                       ItemTemplate="{StaticResource TabItemTemplate}"
                                       ContentTemplate="{StaticResource TabContentTemplate}"/>
    </Grid>
</Window>
